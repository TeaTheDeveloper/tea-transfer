# Exchange-rate API notes

Do not store API keys in this file or in JavaScript.

The application now exposes a small server-side endpoint at:
  /exchange-rates?base=USD

If you use a paid exchange-rate provider, store its credential in the server
environment (for example EXCHANGE_RATE_API_KEY) and call the provider from
the server, never from browser JavaScript.
