# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=tea_transfer
DB_USER=root
DB_PASSWORD=

# Optional: configure a server-side exchange-rate provider if you replace
# the demo public provider in exchange-rates.php.
EXCHANGE_RATE_API_KEY=
